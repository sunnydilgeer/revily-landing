# Lesson 11 source map

Lesson 11 is authored from the four PDF/video pairs in `N11_Ordering_Numbers`.

| Section | Source PDF and video | App coverage |
| --- | --- | --- |
| Ordering decimals | `N11.1_Ordering_Decimals` | Q1 video/worked example, Q2-Q5c practice, aligned decimals and digit-count misconception |
| Ordering large numbers | `N11.2_Ordering_Large_Numbers` | Q1 video/worked example, Q2-Q5c practice, digit-count comparison and open integer interval |
| Ordering negative numbers | `N11.3_Ordering_Negative_Numbers` | Q1 video/worked example, Q2-Q5c practice, magnitude reasoning and open integer interval |
| Fractions, decimals and percentages | `N11.4_Ordering_Fractions_Decimals_Percentages` | Q1 video/worked example, Q2-Q5c practice, common-form comparison and open decimal interval |

The app keeps the source order, contexts, answers and original representations. The three source prompts with a range of valid answers use deterministic interval grading; the two explicitly whole-number ranges additionally require an integer. The final screen is an app-only consolidation summary.
